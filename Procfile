# Paper2Slides requires Docker Compose for deployment
# This Procfile will fail if Nixpacks is used instead of Docker Compose

web: echo "ERROR: Paper2Slides must be deployed with Docker Compose, not Nixpacks." && echo "Please configure Coolify to use 'Docker Compose' as the build pack." && echo "See QUICKSTART_COOLIFY.md or DEPLOYMENT.md for instructions." && exit 1
