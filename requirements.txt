# Core Library Dependencies (paper2slides/)
# RAG Core
huggingface_hub
lightrag-hku

# MinerU for document parsing
# Using base mineru without [core] extra to avoid large CUDA dependencies (~2.5GB)
# MinerU requires pipeline dependencies for document parsing
# Install CPU-only torch FIRST to prevent CUDA version from being installed
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.6.0+cpu
torchvision==0.21.0+cpu

# MinerU pipeline dependencies (without torch which is installed above)
matplotlib>=3.10,<4
ultralytics>=8.3.48,<9
doclayout_yolo==0.0.4
dill>=0.3.8,<1
ftfy>=6.3.1,<7
shapely>=2.0.7,<3
pyclipper>=1.3.0,<2
omegaconf>=2.3.0,<3
transformers>=4.49.0,<5.0.0,!=4.51.0
onnxruntime>1.17.0

# MinerU base package
mineru==2.6.4

tqdm

# Image Processing
Pillow>=10.0.0
reportlab>=4.0.0

# API
openai>=1.0.0
python-dotenv>=1.0.0

# Data Processing
pyyaml>=6.0
requests>=2.28.0


# Web API Dependencies (api/)
fastapi==0.122
uvicorn[standard]==0.38.0
python-multipart==0.0.20
pydantic==2.12.3
